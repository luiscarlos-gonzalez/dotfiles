[user]
	email = lcarlosglez@gmail.com
  name = Luis Carlos González Hernández
[alias]
  com = checkout master
  cob = checkout -b
  co = checkout
  rbi = "!sh -c \"git rebase -i HEAD~$1\" -"
  pfo = push -f origin
  fixup = "!sh -c \"git a && git c \"Fix\" && git rbi $1\" -"
  rbm = rebase master
  s = status
  a = add .
  c = commit -m
[core]
  excludesfile = /home/kentverger/.gitignore
  editor = vim
